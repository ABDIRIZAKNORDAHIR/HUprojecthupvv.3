# Real AI — OpenAI ChatGPT and/or Google Gemini (teacher project analysis & Q&A)
# Add at least ONE key below (see CONFIGURE_REAL_AI.txt)

# Option A — OpenAI ChatGPT (https://platform.openai.com/api-keys)
# OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1

# Option B — Google Gemini (https://aistudio.google.com/apikey)
# GEMINI_API_KEY=your-gemini-key-here
GEMINI_MODEL=gemini-2.0-flash
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta

# Provider: auto (default) | openai | gemini
AI_PROVIDER=auto
# If primary provider fails, try the other (true unless set to false)
AI_FALLBACK=true

# Local MySQL database (default for this project)
DB_DRIVER=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=12345678
DB_DATABASE=ProjectHub

# Admin account
ADMIN_EMAIL=daacadnuur646@gmail.com
ADMIN_DEFAULT_PASSWORD=ProjectHub123!
ADMIN_UNIVERSITY_ID=HU0009000

# Email OTP (Gmail App Password for SMTP_PASS)
# Recipients only see "Hormuud ProjectHub" branding + logo — OTP goes ONLY to their address (no BCC to you).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=daacadnuur646@gmail.com
SMTP_PASS=
SMTP_FROM=Hormuud ProjectHub <daacadnuur646@gmail.com>
OTP_TTL_MINUTES=10

# Web Push (optional � auto-generated if missing; persist for stable subscriptions)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:admin@projecthub.local
