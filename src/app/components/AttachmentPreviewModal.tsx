/** Kept as a safe no-op so leftover references cannot crash the teacher pages. */
export function AttachmentPreviewModal(_props: {
  open?: boolean;
  name?: string;
  data?: string;
  onClose?: () => void;
}) {
  return null;
}
